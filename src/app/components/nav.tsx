"use client";

type Props = {};
export default function Nav({}: Props) {
  return (
    <div className="fixed bottom-0 w-full">
      <nav className="w-full max-w-xl px-3 mx-auto bg-background/30 backdrop-blur-xl py-7">
        Nav
      </nav>
    </div>
  );
}
